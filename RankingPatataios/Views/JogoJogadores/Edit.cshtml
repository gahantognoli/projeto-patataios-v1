@model RankingPatataios.Models.JogoJogador

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>JogoJogador</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="JogoId" class="control-label"></label>
                <select asp-for="JogoId" class="form-control" asp-items="ViewBag.JogoId"></select>
                <span asp-validation-for="JogoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="JogadorId" class="control-label"></label>
                <select asp-for="JogoId" class="form-control" asp-items="ViewBag.JogadorId"></select>
                <span asp-validation-for="JogadorId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
